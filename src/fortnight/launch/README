# select something form every launch group:

robot.launch				Robot hardware
bag.launch				Bag played data

teleop.launch				Logitech joystick teleoperation, can be run over network
# or nothing for fully autonomous operation

mapping.launch				Used for map creation (usually with teleop or remote teleop)
localization.launch			Testing localization (can be used with teleop)
fortnight.launch			Unimplemented -- fully autonomous robot operation
# or nothing for input data rosbag record
